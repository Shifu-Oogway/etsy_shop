fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0.29
asyncpg>=0.29
aiosqlite>=0.20
pgvector>=0.2.5
alembic>=1.13
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
celery>=5.3
redis>=5.0
# Optional renderers — install if available; the file generators degrade
# gracefully when missing, so these never block a Docker build.
reportlab>=4.1
pillow>=10.0
openpyxl>=3.1
# Test tooling
pytest>=8.0
pytest-asyncio>=0.23
